{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev --optimize-autoloader"
    },
    "deploy": {
        "startCommand": "bash -c 'php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan schedule:work & exec php artisan serve --host=0.0.0.0 --port=$PORT'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}
